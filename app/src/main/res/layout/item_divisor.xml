<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="8dp"
    android:paddingTop="18dp"
    android:paddingEnd="8dp"
    android:paddingBottom="6dp">

    <!-- Tarja na cor da categoria (vermelha para alcoólicas) -->
    <View
        android:id="@+id/marcadorSecao"
        android:layout_width="6dp"
        android:layout_height="22dp"
        android:background="@drawable/fundo_marcador" />

    <TextView
        android:id="@+id/txtTituloSecao"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:layout_weight="1"
        android:textColor="@color/texto_primario"
        android:textSize="18sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/txtQuantidadeSecao"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/fundo_pilula"
        android:backgroundTint="@color/superficie_variante"
        android:paddingStart="10dp"
        android:paddingTop="3dp"
        android:paddingEnd="10dp"
        android:paddingBottom="3dp"
        android:textColor="@color/texto_secundario"
        android:textSize="14sp" />

</LinearLayout>
